using CCP.DTO;
using System;
using System.Collections.Generic;
using System.Text;

namespace CCP.Helpers
{
    abstract class DatabaseUtils
    {
        //FW - So, I dont have time to do any actual db stuff, but it'd be something like this...
        //It's doable, but I've only got so much time to kill!

        //static void Read()
        //{
        //    IEnumerable<dynamic> result = db.Query<dynamic>("select age, name, address_line1, address_line2");

        //    foreach(var rows in result)
        //    {
        //        var fields = (IDictionary<string, object>)rows;
        //        foreach(var field in fields)
        //        {
        //            CCPDataItem item = new CCPDataItem();
        //            etc
        //        }
        //    }
        //}

    }
}
